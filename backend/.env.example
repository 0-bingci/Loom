# 复制成 .env 使用
# Postgres 连接串(API 与 worker 共用)
DATABASE_URL=postgres://loom:loom@localhost:5432/loom

# API 鉴权用的静态 token;请求需带 Authorization: Bearer <token>
LOOM_API_TOKEN=change-me

# API 监听端口
LOOM_API_PORT=8787

# worker 轮询间隔(毫秒),默认 60000 = 每分钟
LOOM_WORKER_INTERVAL_MS=60000
